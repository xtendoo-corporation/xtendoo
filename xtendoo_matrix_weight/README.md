# XTD Matrix Weight (Odoo 18)

Módulo para leer peso desde indicador Matrix II por RS-232/RS-485 usando:
- **TCP↔RS** (terminal server) o
- **Serie local** (USB-RS232, requiere `pyserial`).

Configura en Ajustes → Báscula Matrix II y usa el botón en albaranes.