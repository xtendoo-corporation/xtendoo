<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Parámetro de sistema: Límite de productos en POS -->
        <record id="ir_config_parameter_pos_limited_product_count" model="ir.config_parameter">
            <field name="key">point_of_sale.limited_product_count</field>
            <field name="value">500</field>
        </record>

        <!-- Parámetro de sistema: Límite de clientes en POS -->
        <record id="ir_config_parameter_pos_limited_customer_count" model="ir.config_parameter">
            <field name="key">point_of_sale.limited_customer_count</field>
            <field name="value">500</field>
        </record>

    </data>
</odoo>

