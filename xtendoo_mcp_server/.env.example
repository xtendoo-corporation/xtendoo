# Odoo MCP Module Test Configuration
# Copy this file to .env and update with your test environment details

# Test Environment Configuration
# =============================

# Odoo server URL (required)
# The URL where your Odoo instance is running
ODOO_URL=http://localhost:8069

# Database name (required)
# The Odoo database to connect to
ODOO_DB=your-db-name

# Authentication Configuration
# ============================

# API key for authentication (required)
# Generate in Odoo: Settings > Users & Companies > Users > API Keys
ODOO_API_KEY=your-api-key-here

# User ID associated with the API key (required)
# Found in Odoo: Settings > Users & Companies > Users
ODOO_USER_ID=2

# Username for fallback authentication (optional)
# Only used if API key authentication fails
ODOO_USER=admin

# Password for fallback authentication (optional)
# Only used with ODOO_USER
ODOO_PASSWORD=admin

# Test Configuration
# ==================

# Debug mode (optional)
# Set to true for verbose output
DEBUG=false

# Skip rate limit test (optional)
# Rate limit test takes time, set to false to enable
SKIP_RATE_LIMIT_TEST=true

# Clean up test data after tests (optional)
# Set to false to keep test records for debugging
CLEANUP_TEST_DATA=true

# Model Configuration for Testing
# ================================
# These should match your MCP configuration in Odoo

# Model with MCP enabled (required)
MCP_TEST_ENABLED_MODEL=res.users

# Model with read-only permissions (required)
MCP_TEST_READ_ONLY_MODEL=res.country

# Model with read-write permissions (required)
MCP_TEST_READ_WRITE_MODEL=res.partner

# Model with full access permissions (required)
MCP_TEST_FULL_ACCESS_MODEL=res.company

# Model without MCP access (required)
MCP_TEST_DISABLED_MODEL=ir.attachment